/*Criar um algoritmo que capture dois valores informados pelo usuário e faça a soma e apresente o resultado na tela. */

var numero = prompt()*1 ;

var numero1 = prompt()*1 ;

alert(numero + numero1);